# Figma Code Connect — personal access token with "Code Connect: Write" scope.
# Create at: Figma → Settings → Security → Personal access tokens.
# Then: cp .env.example .env  (and fill it in).  Never commit .env.
FIGMA_ACCESS_TOKEN=
